# Vibecoded Webshop (minimal)

This is a minimal React + Vite project implementing a Home page with carousel and three sections (Categories, Our latest arrivals, Our Products) using placeholder images.

Install and run:

```bash
npm install
npm run dev
```

Open `http://localhost:5173`.
